import math
import os
import random
import re
import sys

# Complete the staircase function below.
def staircase(n):
    m=n+1
    for i in range(1, m):
       print(' ' * (m -1- i) + '#' * i)

if __name__ == '__main__':
    n = int(input())

    staircase(n)
